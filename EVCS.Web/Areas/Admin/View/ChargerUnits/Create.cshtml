@model ChargerUnitUpsertDto
@{
    ViewData["Title"] = "Create Charger";
}
<h2 class="mb-3"><i class="bi bi-hdd-stack"></i> Create Charger</h2>
<partial name="_ValidationSummary" />
<form asp-action="Create" method="post" class="mt-3">
    <partial name="_Form" />
    <div class="mt-3 d-flex gap-2">
        <button class="btn btn-primary"><i class="bi bi-check2"></i> Save</button>
        <a asp-action="Index" asp-route-stationId="@Model.StationId" class="btn btn-outline-secondary">Cancel</a>
    </div>
</form>
@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}