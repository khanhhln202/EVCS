@model ConnectorPortUpsertDto
@{
    ViewData["Title"] = "Create Port";
}
<h2 class="mb-3"><i class="bi bi-plug"></i> Create Port</h2>
<partial name="_ValidationSummary" />
<form asp-action="Create" method="post" class="mt-3">
    <partial name="_Form" />
    <div class="mt-3 d-flex gap-2">
        <button class="btn btn-primary"><i class="bi bi-check2"></i> Save</button>
        <a asp-action="Index" asp-route-chargerId="@Model.ChargerId" class="btn btn-outline-secondary">Cancel</a>
    </div>
</form>
@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}