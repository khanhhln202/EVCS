@model EVCS.Services.DTOs.ConnectorPortUpsertDto
@{
    ViewData["Title"] = "Thêm cổng sạc";
}
<div class="card shadow-sm">
    <div class="card-body">
        <form asp-action="Create" method="post" class="vstack gap-3" novalidate>
            <partial name="_Form" />
            <div class="d-flex gap-2">
                <button class="btn btn-primary"><i class="bi bi-save"></i> Lưu</button>
                <a asp-action="Index" asp-route-chargerId="@Model.ChargerId" class="btn btn-outline-secondary">Huỷ</a>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}