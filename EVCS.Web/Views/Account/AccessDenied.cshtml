@{
    ViewData["Title"] = "Truy cập bị từ chối";
    var returnUrl = ViewData["ReturnUrl"] as string;
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0">
                <div class="card-body text-center p-5">
                    <div class="mb-4">
                        <i class="bi bi-shield-exclamation text-danger" style="font-size: 5rem;"></i>
                    </div>
                    
                    <h2 class="fw-bold text-danger mb-3">Truy cập bị từ chối</h2>
                    
                    <p class="text-muted mb-4">
                        Bạn không có quyền truy cập vào tài nguyên này.
                    </p>

                    @if (!string.IsNullOrEmpty(returnUrl))
                    {
                        <div class="alert alert-warning">
                            <i class="bi bi-info-circle"></i>
                            Bạn đã cố gắng truy cập: <code>@returnUrl</code>
                        </div>
                    }

                    <div class="d-flex flex-column gap-2">
                        <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
                            <i class="bi bi-house"></i> Về trang chủ
                        </a>

                        @if (User.Identity?.IsAuthenticated == true)
                        {
                            <a asp-controller="Profile" asp-action="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-person-circle"></i> Trang cá nhân
                            </a>
                        }
                        else
                        {
                            <a asp-controller="Account" asp-action="Login" asp-route-returnUrl="@returnUrl" class="btn btn-outline-primary">
                                <i class="bi bi-box-arrow-in-right"></i> Đăng nhập
                            </a>
                        }
                    </div>

                    <hr class="my-4" />

                    <p class="small text-muted mb-0">
                        Nếu bạn nghĩ đây là lỗi hệ thống, vui lòng liên hệ quản trị viên.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>